# Assets Guide

Optional sample images for the Dataset Overview tab:

- `assets/sample_images/melanoma/`
- `assets/sample_images/nevus/`

Add a few `.jpg`, `.jpeg`, or `.png` files into each folder and the app will show them automatically.

If these folders are empty, the app will display a placeholder message instead.
